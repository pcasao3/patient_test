<%= form_for(fhcn_patients_controller) do |f| %>
  <% if fhcn_patients_controller.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fhcn_patients_controller.errors.count, "error") %> prohibited this fhcn_patients_controller from being saved:</h2>

      <ul>
      <% fhcn_patients_controller.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
